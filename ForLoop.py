cities = ['new york city', 'mountain view', 'chicago', 'los angeles']
capitalized_cities = []

for city in cities: # for loop to loop through the citites list, uses city but can be I, similar to C
    capitalized_cities.append(city.title()) # uses the title() command to titleise the variables (adds a capital at start)
    # uses append to add to the new list
    print (capitalized_cities)

c